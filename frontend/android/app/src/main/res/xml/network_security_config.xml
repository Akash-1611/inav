<?xml version="1.0" encoding="utf-8"?>
<network-security-config>
    <!-- Allow cleartext traffic for the API server -->
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">16.170.238.47</domain>
    </domain-config>
    <!-- For development, you can also allow all cleartext (not recommended for production) -->
    <!-- Uncomment below if you need to allow cleartext for all domains -->
    <!--
    <base-config cleartextTrafficPermitted="true">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
    -->
</network-security-config>

